
<%= form_for(@employee, as: :employee, url: "employee/add_path", remote: true, method: "get", html: {class: "form-horizontal"}) do |e|%>
  <lable> name</lable><%= e.text_field :name %><br>
  <lable> role</lable><%= e.select :role, options_for_select(["Select role","CEO","Director","VP", "Manager", "SDE"]), {class: "form-control"} %><br>
  <lable> Salary</lable><%= e.text_field :salary %><br>
  <lable> Rating</lable><%= e.text_field :rating %><br>
  <%= e.submit "Add Details" %>
  
<% end %>

<script type="text/javascript">
	
	$(function(){	

	})	
</script>